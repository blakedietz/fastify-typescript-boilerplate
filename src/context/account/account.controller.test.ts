// TODO: (bdietz) - fix
// import tap from 'tap';
// import { hashPassword, comparePasswordHash } from './account.controller';
//
// tap.test('Accounts.hashedPassword', async t => {
//   const clearTextPassword = 'test12345';
//   const hashedPassword = await hashPassword({ password: clearTextPassword });
//   const result = await comparePasswordHash({
//     hashedPassword,
//     clearTextPassword,
//   });
//
//   t.equal(result, true);
// });
